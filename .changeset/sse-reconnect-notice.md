---
"@slashgear/roadmap-maker": patch
---

Show a "Connection lost — reconnecting…" banner in team mode when the SSE stream drops. The banner disappears automatically once the connection is restored and the server sends a fresh `init` event.
