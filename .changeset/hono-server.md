---
"@slashgear/roadmap-maker": patch
---

Replace Caddy with a Hono static file server (`server/`). Supports precompressed assets (br/zstd/gzip), immutable cache headers for versioned assets, and security headers.
