---
'@slashgear/roadmap-maker': minor
---

Migrate team mode from SQLite to PostgreSQL. The server now connects via `DATABASE_URL` (postgres tagged-template client). Docker Compose updated with postgres service and app profiles. README and CONTRIBUTING updated accordingly.
